# 功能验收清单

每个 feature 完成时，除对应测试命令外，还应检查本文件中的人工验收项。

## F-00 项目骨架

- `package.json` 存在。
- `npm install` 可成功。
- `npx vitest run` 可成功。
- `./init.sh` 在实现脚手架存在时真实执行类型检查和测试。

## F-01 项目数据模型与 Store

- 可以表达背景图引用、配件列表、事件规则和 schemaVersion。
- 可以新增、更新、删除配件。
- 可以选择当前配件并更新其状态字段。
- Maker 项目模板和 Runner 运行状态相互分离。

## F-02 地图面板背景图画布

- 可以导入或引用一张桌游地图面板背景图。
- 背景图在 PixiJS 画布中正确显示。
- 画布支持基础缩放和平移。
- 坐标转换不依赖小地图、网格或节点图。

## F-03 桌游配件编辑工具

- 可以放置通用配件。
- 可以选择、移动、删除配件。
- 配件 kind 为用户自定义语义，不写死 NPC、建筑、雷达目标或地块。
- 无效操作有明确反馈。

## F-04 配件 Inspector

- 选中配件后字段正确显示。
- 保存通过 Store action。
- 危险操作有明确视觉和确认。

## F-05 事件规则编辑器

- 可以创建 trigger、conditions、effects 和 repeatable。
- trigger 至少支持点击配件、移动配件、进入阶段、状态变化和手动触发。
- effect 至少支持弹出窗口、修改配件状态、显示提示和推进阶段。
- 事件引用不存在的配件时校验失败。

## F-06 事件弹窗系统

- 可以显示剧情/说明文本窗口。
- 可以显示投骰判定窗口。
- 可以显示选择分支窗口。
- 可以显示规则提示窗口。
- 窗口内容来自事件规则，而不是硬编码。

## F-07 Runner 运行模拟

- Runner 从项目模板创建独立运行状态。
- 玩家操作配件会触发匹配事件。
- 已触发事件和配件状态变化记录在运行状态中。
- Runner 不直接修改 Maker 项目模板。

## F-08 Save/Load JSON

- 导出 JSON 包含 `schemaVersion`。
- 导出内容包含背景图引用、配件、事件规则和基础元数据。
- 导入无效 schema 或无效引用会失败并给出错误。
- 暂不要求远端数据库或 Supabase。
