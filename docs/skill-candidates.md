# Skill Candidate Log

Use `$lorecanvas-pattern-miner` to maintain this log. A candidate needs at least
three observed occurrences before promotion.

| Candidate | Occurrences | Stable inputs/outputs | Best surface | Status |
| --- | ---: | --- | --- | --- |
| Windows harness recovery around locked Vite/Rolldown files | 7+ | Registered repository root + wrapper/child PID + args, full harness, optional restart | `scripts/dev-server.mjs` + `scripts/with-vite-stopped.ps1` | promoted |
| Browser evidence capture for PixiJS flows | 3+ | Stable evidence schema | `$lorecanvas-browser-qa` | promoted |
| Feature planning, delegation, repair, and closure | 3+ | Stable lifecycle | `$lorecanvas-feature-loop` | promoted |

When adding a row, link the supporting entries in `progress.md` or feature
evidence. Do not promote a workflow solely because it is annoying once.
